<%= form_with(model: @blog, local: true, url: choose_new_or_edit ) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
        <% blog.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
 <br>



<div class="field">
     <p> <%= form.label :title %> </p>
     <p><%= form.text_field :title %> </p>
  </div> </p>

<div class="field">
     <p> <%= form.label :content %> </p>
     <p><%= form.text_area :content, size: "22x5" %> </p>
  </div> </p>

<div class="image">
     <p><%= form.label :image %></p>
     <p><%= image_tag(@blog.image.url) if @blog.image && @blog.image.url %></p>
     <p><%= form.file_field :image, class: 'image', id: 'image' %></p>
     <p><%= form.hidden_field :image_cache, class: 'image' , id: 'image' %></p>
</div>
<br>

<% end %>